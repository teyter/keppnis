#include <bits/stdc++.h>
#define rep(E, F) for (E = 0; E < (F); E++)
using namespace std;
typedef vector<int> vi;
typedef vector<vi> vvi;

int main()
{
    vi v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    int x = v.back().back();

    cout << x << endl;

}
